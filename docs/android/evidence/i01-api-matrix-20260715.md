# I01 Android API-level emulator matrix — 2026-07-15

## Matrix

| AVD | Android | Role in matrix | Result |
| --- | --- | --- | --- |
| `MedicationApp_API_26` | 8.0 / API 26 | declared minSdk, legacy storage/runtime behavior | 76/76 |
| `MedicationApp_API_33` | 13 / API 33 | runtime notification permission boundary | 76/76 |
| `MedicationApp_API_35` | 15 / API 35 | current target behavior | 76/76 |

Command: `./gradlew connectedDebugAndroidTest` with all three AVDs booted. Final result: 228/228 instrumentation tests passed.

## Findings and corrections

The first cross-API run exposed test infrastructure assumptions rather than production failures:

- `FileInputStream.readNBytes` was unavailable on API 26; the PDF signature assertion now uses the minSdk-compatible `read(byte[])` path.
- A screenshot fixture wrote through scoped-storage MediaStore APIs and required legacy external-storage permission on API 26; fixtures now stay in private app cache and do not pollute user media.
- Several LazyColumn assertions assumed API 35 viewport composition and counted off-screen duplicate nodes. They now scroll through the production container or assert semantics/actions independently of viewport height.

After the corrections, the previously failing focused classes passed on all three APIs, followed by the complete 228-test green run. No production UI/API behavior was weakened to satisfy the matrix.

This is emulator evidence only. Physical notification delivery/Doze/tap/process death, OEM lifecycle, TalkBack, font scaling, browser/share surfaces and device transfer remain separate Gate I requirements.
