# I01 Android API-level emulator matrix — 2026-07-15

## Matrix

| AVD | Android | Role in matrix | Result |
| --- | --- | --- | --- |
| `MedicationApp_API_26` | 8.0 / API 26 | declared minSdk, compact 1080 x 1920 viewport, legacy storage/runtime behavior | 259/259 |
| `MedicationApp_API_33` | 13 / API 33 | runtime notification permission boundary | 259/259 |
| `MedicationApp_API_35` | 15 / API 35 | current target behavior | 259/259 |

Command: `./gradlew connectedDebugAndroidTest`, run against each AVD. C57 final current result: 777/777 instrumentation tests passed with 0 failed and 0 skipped. API 26 completed in 3m41s, API 33 in 7m42s, and API 35 in 7m08s. The earlier C36 187-per-API and historical 108-test matrices remain useful historical evidence but are superseded by this expanded run.

An additional large-phone layout pass overrides the API-35 AVD to 1344 x 2992 pixels at 480 dpi (448 x 997 dp) and passes the same 108/108 suite. The override is reset afterward. This complements the compact API-26 411 x 731 dp and standard API-35 411 x 914 dp viewports without treating an emulator override as physical-device evidence.

## Findings and corrections

The first cross-API run exposed test infrastructure assumptions rather than production failures:

- `FileInputStream.readNBytes` was unavailable on API 26; the PDF signature assertion now uses the minSdk-compatible `read(byte[])` path.
- A screenshot fixture wrote through scoped-storage MediaStore APIs and required legacy external-storage permission on API 26; fixtures now stay in private app cache and do not pollute user media.
- Several LazyColumn assertions assumed API 35 viewport composition and counted off-screen duplicate nodes. They now scroll through the production container or assert semantics/actions independently of viewport height.

The expanded adaptive suite then exposed a real compact-viewport issue in the PDF custom-range sheet: its validation and generate action could fall below the non-scrollable API-26 viewport. The production sheet is now vertically scrollable and the regression test scrolls to both targets.

After the corrections, the focused classes passed on all three APIs, followed by the complete matrix on the final code. The 2026-07-15 XP-007 extension added five production-Compose stale/retry scenarios. C06 then added four 200%-font Caregiver reachability scenarios, real `MainActivity` recreation and portrait/landscape round trips for both Patient and Caregiver shells, merged Caregiver History day semantics, and medication-specific TalkBack labels for Today record/cancel, medication edit and inventory detail. `CaregiverConfigurationUiTest` selects Inventory in the production shell and verifies that both the selected tab and its content survive recreation and both rotations. `CaregiverLargeTextUiTest` now executes Today, medication, inventory and History action paths in both light and dark themes at 200% font scale. C01 adds explicit 200%-font Analytics-dialog action reachability and opt-out state verification, Patient-link long-error/action reachability, Caregiver confirmation-required/resend reachability, duplicate-submit loading locks and auth-form exit/re-entry state reset.

C36 reran the expanded 187-test suite after C32 streak, C33 current Caregiver Today and C34 missed-dose routing. API 26 exposed only test-harness assumptions: Compose cannot capture a dialog node with `captureToImage` before API 28, and several lazy-list tests assumed that an off-screen item was already composed. The fixtures now use a device screenshot below API 28 and the action tests scroll the actual production list/detail container to the target. This verifies compact-screen reachability instead of relying on API-35 viewport composition. No production UI/API behavior was changed to satisfy the rerun.

C57 reran the complete post-C56 259-test suite. The larger matrix exposed the same class of compact API-26 assumption in seven newer auth/link/Today/medication adaptive checks. Stable semantics tags were added to the existing auth-choice and patient-link surfaces, and tests now scroll the actual production lazy lists before each interaction. The corrected final matrix passes 259/259 on every API (777/777); details are recorded under `c57-20260716/`.

This is emulator evidence only. Physical notification delivery/Doze/tap/process death, OEM lifecycle, TalkBack, font scaling, browser/share surfaces and device transfer remain separate Gate I requirements.
