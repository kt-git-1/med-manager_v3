import { describe, it } from "vitest";

describe("dose record event RLS", () => {
  it.skip("prevents non-owned caregivers from reading events", () => {
    // RLS policies are implemented in a later task (T013+).
  });
});
