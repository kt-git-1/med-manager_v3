import { describe, it } from "vitest";

describe("dose record event emission", () => {
  it.skip("creates an event when a TAKEN record is created", () => {
    // Event emission is implemented in a later task (T010+).
  });
});
