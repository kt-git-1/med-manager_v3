import SwiftUI

struct CaregiverHistoryView: View {
    var body: some View {
        Text("Caregiver History")
            .font(.headline)
            .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .center)
            .accessibilityIdentifier("CaregiverHistoryView")
    }
}
